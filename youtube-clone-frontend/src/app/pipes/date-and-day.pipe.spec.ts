import { DateAndDayPipe } from './date-and-day.pipe';

describe('DateAndDayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAndDayPipe();
    expect(pipe).toBeTruthy();
  });
});
