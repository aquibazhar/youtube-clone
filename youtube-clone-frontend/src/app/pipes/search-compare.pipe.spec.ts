import { SearchComparePipe } from './search-compare.pipe';

describe('SearchComparePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchComparePipe();
    expect(pipe).toBeTruthy();
  });
});
